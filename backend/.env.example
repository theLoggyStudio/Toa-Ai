# Tarification
PRICE_BASE_CFA=200
PRICE_PER_BUBBLE_CFA=25
# true = bypass PayDunya (bouton "Démarrer la traduction" sans paiement)
# false = paiement PayDunya requis (sandbox si PAYDUNYA_MODE=test, live si production)
DISABLE_PAYMENT=false

# PayDunya (ne jamais committer le .env réel)
# test = sandbox | production = live
PAYDUNYA_MODE=test
# Fresco (restauration) : mode séparé (défaut sandbox)
PAYDUNYA_FRESCO_MODE=test
PAYDUNYA_MASTER_KEY=
PAYDUNYA_TEST_PUBLIC_KEY=
PAYDUNYA_TEST_PRIVATE_KEY=
PAYDUNYA_TEST_TOKEN=
PAYDUNYA_PROD_PUBLIC_KEY=
PAYDUNYA_PROD_PRIVATE_KEY=
PAYDUNYA_PROD_TOKEN=

# Frontend (Vercel) pour CORS et redirections PayDunya
# Effacement texte : bbox max 8 % de la page ; seuil encre < 100 (plus strict = moins de degats)
MAX_BBOX_PAGE_RATIO=0.08
INK_DARK_THRESHOLD=100

FRONTEND_PORT=3100
BACKEND_PORT=9400
FRONTEND_ORIGIN=http://localhost:3100
BACKEND_PUBLIC_URL=http://127.0.0.1:9400

# Cursor API (traduction)
CURSOR_API_KEY=
CURSOR_MODEL=auto
# Optionnel: dossier du workspace pour l'agent local Cursor SDK
CURSOR_WORKSPACE_DIR=../
CURSOR_USE_CLOUD=true
CURSOR_MAX_IMAGE_PX=1920
CURSOR_PAGE_DELAY_SEC=0.5
BATCH_PAGE_SIZE=5
ESTIMATED_BUBBLES_PER_PAGE=4

# Pages traduites en parallèle dans un lot (attente réseau Cursor)
PIPELINE_PAGE_CONCURRENCY=5
# Tâches traitées simultanément (au-delà : file d'attente)
PIPELINE_MAX_CONCURRENT_TASKS=1
# Tentatives Cursor par page avant repli sur la page originale
PAGE_TRANSLATION_ATTEMPTS=2
# Mascotte Toa (Chibie) : false = désactivée (le code reste en place)
CHIBIE_ENABLED=false

# Rendu immersion: texte vertical (caractere par caractere, haut -> bas)
IMMERSION_VERTICAL_TEXT=false

# Police custom (optionnel): chemin relatif depuis backend/ ou chemin absolu
# Exemple relatif: MANGA_FONT_PATH=assets/fonts/WildWordsRoman.ttf
MANGA_FONT_PATH=

# Preset auto (optionnel): police dialogue vs onomatopees
MANGA_DIALOGUE_FONT_PATH=
MANGA_SFX_FONT_PATH=

# Dictionnaires RAG (JMdict japonais + Kengdic coréen, téléchargés dans data/glossaries/)
GLOSSARY_RAG_ENABLED=true
